apiVersion: v1
kind: Service
metadata:
  name: task-manager
  namespace: task-manager

spec:
  type: NodePort

  selector:
    app: task-manager

  ports:
    - name: http
      port: 8080
      targetPort: 8080
      nodePort: 30080